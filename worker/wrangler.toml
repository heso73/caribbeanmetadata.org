name = "cms-api"
main = "worker.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "production"

routes = [
  { pattern = "api.caribbeanmetadata.org/*", custom_domain = true }
]
